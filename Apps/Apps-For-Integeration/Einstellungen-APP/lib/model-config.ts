// Kept identical to the central settings app so the reader never falls back to
// a different model than the one the learner sees in the settings screen.
export const DEFAULT_OPENAI_MODEL = "gpt-5.6-sol";
